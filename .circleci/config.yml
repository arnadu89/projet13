version: 2

jobs:
  build:
    working_directory: /app
    docker:
      - image: docker:stable
    steps:
      - checkout
      - run:
          name: Build docker image
          command: docker build -t oc_projet13:$CIRCLE_SHA1 .

  push_container:
    docker:
      - image: docker:stable

    steps:
      - checkout
      - run:
          name: Log in DockerHub
          command: |
            echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin

      - run:
          name: Push container to DockerHub
          command: |
            docker tag oc_projet13:$CIRCLE_SHA1 $DOCKERHUB_USERNAME/oc_projet13:$CIRCLE_SHA1
            docker push $DOCKERHUB_USERNAME/oc_projet13:$CIRCLE_SHA1
  test:
    docker:
      - image: oc_projet13:$CIRCLE_SHA1
    working_directory: /projet13
    steps:
      - checkout
      - run:
          name: Run tests
          command: pytest

workflows:
  version: 2
  build_test_push:
    jobs:
      - build
      - test:
          requires:
            - build
      - push_container:
          requires:
            - test
          filters:
            branches:
              only: master
